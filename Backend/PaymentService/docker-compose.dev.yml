services:

  mysql-payment:
    image: mysql:8.0
    container_name: mysql-payment-dev
    environment:
      MYSQL_ROOT_PASSWORD: senha
      MYSQL_DATABASE: payment_db
    ports:
      - "3309:3306"
    volumes:
      - mysql-payment-dev-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - rede-microsservicos

  payment-service:
    image: eclipse-temurin:21-jdk-alpine
    container_name: payment-service-dev
    working_dir: /app
    volumes:
      - ./:/app
      - ~/.m2:/root/.m2
    command: ./mvnw spring-boot:run
    ports:
      - "8200:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-payment:3306/payment_db?createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: senha
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: com.mysql.cj.jdbc.Driver
      JWT_SECRET: zdtlD3G/O/9n8u0K1XyBwH2zYjL+mRQ5aV5Z+B4TqP0=
      JWT_EXPIRATION_MS: 1440
      EUREKA_URL: http://eureka-server:8761/eureka/
    depends_on:
      mysql-payment:
        condition: service_healthy
    networks:
      - rede-microsservicos

volumes:
  mysql-payment-dev-data: