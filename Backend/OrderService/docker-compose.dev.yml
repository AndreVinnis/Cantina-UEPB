services:

  mysql-order:
    image: mysql:8.0
    container_name: mysql-order-dev
    environment:
      MYSQL_ROOT_PASSWORD: senha
      MYSQL_DATABASE: order_db
    ports:
      - "3308:3306"
    volumes:
      - mysql-order-dev-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - rede-microsservicos

  order-service:
    image: eclipse-temurin:21-jdk-alpine
    container_name: order-service-dev
    working_dir: /app
    volumes:
      - ./:/app
      - ~/.m2:/root/.m2
    command: ./mvnw spring-boot:run
    ports:
      - "8100:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-order:3306/order_db?createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: senha
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: com.mysql.cj.jdbc.Driver
      JWT_SECRET: zdtlD3G/O/9n8u0K1XyBwH2zYjL+mRQ5aV5Z+B4TqP0=
      JWT_EXPIRATION_MS: 1440
      EUREKA_URL: http://eureka-server:8761/eureka/
    depends_on:
      mysql-order:
        condition: service_healthy
    networks:
      - rede-microsservicos

volumes:
  mysql-order-dev-data: